var inc = 0.01;

function setup() {
  createCanvas(200, 200);
}

function draw() {
  loadPixels();
  background(51);
  stroke(255);
  noFill();
  beginShape();
  for(var x = 0; x < width; x++) {

  }
  endShape();

  start += inc;
}
